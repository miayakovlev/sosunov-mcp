apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sosunov-mcp.fullname" . }}-config
  labels:
    {{- include "sosunov-mcp.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
